services:
  node-checker:
    build: .
    container_name: node-checker
    restart: unless-stopped
    env_file: .env
    environment:
      STATE_FILE: /data/state.json
    volumes:
      - ./data:/data
